# Resource Quota for namespace
apiVersion: v1
kind: ResourceQuota
metadata:
  name: restaurant-quota
  namespace: restaurant-bot
spec:
  hard:
    requests.cpu: "10"
    requests.memory: "20Gi"
    limits.cpu: "20"
    limits.memory: "40Gi"
    persistentvolumeclaims: "10"
    services.loadbalancers: "2"
---
# Limit Range for pods
apiVersion: v1
kind: LimitRange
metadata:
  name: restaurant-limits
  namespace: restaurant-bot
spec:
  limits:
  - max:
      cpu: "2"
      memory: "4Gi"
    min:
      cpu: "50m"
      memory: "64Mi"
    default:
      cpu: "500m"
      memory: "512Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    type: Container
